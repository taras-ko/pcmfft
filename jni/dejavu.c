#include <stdlib.h>

#include "dejavu.h"
#include "getopt.h"

#include "database.h"
#include "fingerprint.h"

enum Actions {
};

int main(int argc, char *argv[])
{
	parse_options(argc, argv);

	exit(EXIT_SUCCESS);
}
